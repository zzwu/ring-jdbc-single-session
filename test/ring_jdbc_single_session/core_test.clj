(ns ring-jdbc-single-session.core-test
  (:require [clojure.test :refer :all]
            [ring-jdbc-single-session.core :refer :all]))

(deftest a-test
  (testing "FIXME, I fail."
    (is (= 0 1))))
